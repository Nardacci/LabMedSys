import './style.css';
import { createClient } from '@supabase/supabase-js';
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY);
const app=document.querySelector('#app');
const shell=(title,c)=>`<main><section class="brand"><div><span class="eyebrow">LABMEDSYS</span><h1>Pharmaceutical operations, connected.</h1><p>A secure workspace for the next generation of pharmaceutical industry.</p></div></section><section class="panel"><div class="card"><div class="eyebrow">LABMEDSYS</div><h2>${title}</h2>${c}</div></section></main>`;
function login(){app.innerHTML=shell('Welcome back',`<p>Access your workspace.</p><form id="login"><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required></label><button>Sign in</button></form><div class="divider"></div><p>New to LabMedSys? <a href="#signup">Create your workspace</a></p><small id="msg"></small>`);document.querySelector('#login').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);const {error}=await supabase.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});document.querySelector('#msg').textContent=error?.message||'';if(!error)route()}}
function signup(){app.innerHTML=shell('Create your workspace',`<p>Start with your company and administrator account.</p><form id="signup"><label>Full name<input name="name" required></label><label>Company name<input name="company" required></label><label>Work email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="6" required></label><button>Create workspace</button></form><p><a href="#login">Back to sign in</a></p><small id="msg"></small>`);document.querySelector('#signup').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);const {data,error}=await supabase.auth.signUp({email:f.get('email'),password:f.get('password'),options:{data:{full_name:f.get('name'),company_name:f.get('company')}}});const msg=document.querySelector('#msg');if(error){msg.textContent=error.message;return;}msg.textContent=data.user?'Workspace created. Confirm your email if required.':'Unable to create workspace.';}}
async function dashboard(){const {data:{user}}=await supabase.auth.getUser();if(!user)return login();const {data:roles}=await supabase.from('user_roles').select('company_id,roles(code)').eq('user_id',user.id);const admin=roles?.some(x=>x.roles?.code==='super_admin');app.innerHTML=`<div class="dashboard"><header><b>LABMEDSYS</b><span>${admin?'Platform Administration':'Workspace'}</span><button id="out">Sign out</button></header><div class="empty"><span class="eyebrow">${admin?'ADMIN DASHBOARD':'DASHBOARD'}</span><h1>${admin?'Platform administration':'Your workspace is ready.'}</h1><p>Package 01 — SaaS Foundation</p></div></div>`;document.querySelector('#out').onclick=async()=>{await supabase.auth.signOut();location.hash='login'}}
async function route(){if(location.hash==='#signup')signup();else await dashboard()}window.addEventListener('hashchange',route);route();